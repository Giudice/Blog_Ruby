<%= form_for @commentaire, url: [@commentaire.post, @commentaire] do |f| %>
  <% if @commentaire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commentaire.errors.count, "error") %> prohibited this commentaire from being saved:</h2>

      <ul>
      <% @commentaire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contenu %><br>
    <%= f.text_area :contenu %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :auteur %><br>
    <%= f.text_field :auteur %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
